// arduino_secrets.h.example — MODÈLE des identifiants WiFi (committable).
// Copie ce fichier en "arduino_secrets.h" (même dossier) et renseigne tes vraies valeurs.
// Le vrai arduino_secrets.h est ignoré par git : le mot de passe ne part jamais avec le code.
#pragma once

#define WIFI_SSID "NOM_DU_RESEAU_WIFI"
#define WIFI_PASS "MOT_DE_PASSE_WIFI"
